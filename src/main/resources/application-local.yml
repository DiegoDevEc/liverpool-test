spring:
  mongodb:
    uri: "${MONGODB_URI:localhost:27017}"
  data:
    redis:
      url: "${REDIS_URL:localhost:6379}"

copomex:
  api:
    token: ${COPOMEX_TOKEN:pruebas}

logging:
  level:
    com.liverpool: DEBUG
    org.springframework.data.mongodb: DEBUG
